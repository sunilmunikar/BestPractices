@model MvcDemos.Features.Setting.SettingsViewModel
@{
    Layout = null;
}

(function () {
    angular.module('@Model.AngularModuleName', [])
    .constant('settings', @Html.Raw(Model.SettingsJson));
    })();